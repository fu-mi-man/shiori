---
name: submit-pr
description: コード品質チェックからPR作成・レビューまでを一括実行するスキル。「PRを出したい」「プルリク作って」「レビューに出したい」「submit」「PRまで一気にやって」「pushしてPR出して」「レビューまでお願い」「PR出すところまでやって」「ブランチを切ってPRまで出して」「コミットしてPRを出して」「ブランチ切ってコミットしてプルリクまで出して」など、ユーザーがブランチ作成・コミット・PR提出のいずれかの意図を示したときに使用する。ブランチ作成からPR作成・レビューまでの一連のワークフローを自動化する。
---

# submit-pr

コミット済みの変更に対して、コード品質チェック → push → PR作成 → code-review を一気通貫で実行する。

## 実行フロー

以下のステップを順番に実行する。各ステップで問題が発生した場合はユーザーに報告して判断を仰ぐ。

### Step 1: コード品質チェック（simplify）

`/simplify` を実行して、変更したコードの品質を確認する。

- 指摘事項がなければ Step 2 に進む
- 指摘事項がある場合、内容をユーザーに提示して確認を取る
  - ユーザーが修正を承認 → 修正を適用してコミット
  - ユーザーがスキップを選択 → そのまま Step 2 に進む

### Step 2: push

main との差分が混入しないよう、push 前に最新化を行う。
特に worktree で作業している場合、元にした main が古いとマージ済みの変更が PR の差分に混入するため、rebase による最新化が重要になる。

1. `git fetch origin` を実行
2. `origin/main` が自分のブランチの起点より進んでいるか確認する
   - 進んでいる → `git rebase origin/main` を実行
     - 競合が出たらユーザーに報告し、解決するか `git rebase --abort` で中断するか判断を仰ぐ
   - 進んでいない → rebase しない
3. `git push` を実行（既にリモートに push 済みの場合は `--force-with-lease` を付ける）

### Step 3: PR作成

1. `.github/pull_request_template.md` のテンプレートを使って PR を作成する
2. コミットの内容からテンプレートの各セクションを埋める
3. `gh pr create` で PR を作成する

### Step 4: code-review

PR作成後、code-review プラグインを実行してレビューを受ける。

## 注意事項

- main ブランチ上では実行しない（ブランチが feat/xxx, fix/xxx 等であることを確認）
- コミットがない場合はユーザーに通知して終了する
